# vision v1.3 验证记录

- Release 构建通过，EXE 与安装器版本为 1.3.0.0。
- 16 组播放核心回归通过，增加空闲无周期刷新的检查。
- 16 组离屏界面测试通过：4 种视频/旋转布局 × 4 种 DPI。
- 八方向边缘检测及 Qt 光标事件分发通过；中心区域不触发缩放。
- 相邻窗口左右/上下对齐、负屏幕坐标和远距离不吸附的矩形计算通过。
- 单击下一帧精确一次、左右长按连续步进、释放停止新增帧请求通过。
- 用户 PNG 与嵌入资源 SHA256 一致；Windows EXE 图标提取确认原始蓝色，图标资源具有多尺寸。
- 查看离屏 UI 渲染图确认窄黑边、方形控件、矢量播放按钮和单排控制栏。
- 安装包完整性、解包和应用 SHA256 一致性验证通过，见 dist/delivery-v1.3.json。

未执行真实桌面拖动、跨进程窗口吸附操作、真实 GPU/扬声器输出或安装/卸载。请手动验收这些系统交互。

测试未显示任何播放器/安装器窗口，没有播放声音，没有改变系统默认应用或操作用户正在使用的窗口。
